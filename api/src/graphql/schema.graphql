type User {
  id: ID!
  name: String!
  email: String!
}

type Message {
  id: ID!
  content: String!
  createdAt: String!
}

type Query {
  users: [User!]!
  messages: [Message!]!
}

type Mutation {
  sendMessage(
    content: String!
  ): Message
}

type Subscription {
  messageSent: Message
}
